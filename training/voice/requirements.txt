# Meera Voice — Moshi Speech-to-Speech (Apple Silicon)
# Use MLX version only (no torch needed on Mac)

# Moshi MLX (Apple Silicon optimized, no torch dependency)
moshi_mlx>=0.2.2

# Audio processing
numpy>=1.24.0
soundfile>=0.12.0
librosa>=0.10.0

# Streaming
websockets>=12.0

# Model download
huggingface_hub>=0.20.0
tqdm>=4.65.0
